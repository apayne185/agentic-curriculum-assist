# Which LLM backend to use: "anthropic" (default) or "gemini"
LLM_PROVIDER=anthropic

# Required when LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# Required when LLM_PROVIDER=gemini (free tier available at aistudio.google.com)
GEMINI_API_KEY=
